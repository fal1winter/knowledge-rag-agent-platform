fastapi>=0.110
pydantic>=2
uvicorn>=0.27

# Optional production adapters. The platform can be inspected without these
# services running, while adapter files keep their integration points explicit.
pymilvus>=2.3
elasticsearch>=7.17
neo4j>=5
sentence-transformers>=2.2
pypdf>=4
python-docx>=1.1
python-pptx>=0.6
pandas>=2
openpyxl>=3

